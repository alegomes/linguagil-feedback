<html>

<head>
	<title>LinguÁgil - Balanço Geral</title>
	<meta http-equiv="Content-Type"  content="text/html; charset=utf-8" />
	<link rel="stylesheet" href="/css/main.css" type="text/css" charset="utf-8" media="screen">
	
	<!-- Google Analytics -->
	<!-- TODO [ALE] Use any sort of JS lib -->
	<script type="text/javascript">

	  var _gaq = _gaq || [];
	  _gaq.push(['_setAccount', 'UA-18637644-1']);
	  _gaq.push(['_trackPageview']);

	  (function() {
	    var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
	    ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
	    var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
	  })();

	</script>
	
</head>

<p id="title">@linguagil</p>

<!-- TODO [ALE] Duplicated code in <ul> -->

<div id="good" class="feedback">
	<ul>
	<% @good_tweets.each do |tweet| %>
		<li>
			<img src='<%= tweet.profile_image_url %>' />
			<span><a href="http://twitter.com/<%= tweet.from_user %>" target="_blank">@<%= tweet.from_user %></a></span>
			<span><%= tweet.text %></span>
		</li>
	<% end %>
	</ul>
</div>

<div id="notsogood" class="feedback">
	<ul>
	<% @bad_tweets.each do |tweet| %>
		<li>
			<img src='<%= tweet.profile_image_url %>' />
			<span><a href="http://twitter.com/<%= tweet.from_user %>" target="_blank">@<%= tweet.from_user %></a></span>
			<span><%= tweet.text %></span>
		</li>
	<% end %>	
	</ul>
</div>

<div id="suggestion" class="feedback">
	<ul>
	<% @suggestion_tweets.each do |tweet| %>
		<li>
			<img src='<%= tweet.profile_image_url %>' />
			<span><a href="http://twitter.com/<%= tweet.from_user %>" target="_blank">@<%= tweet.from_user %></a></span>
			<span><%= tweet.text %></span>
		</li>
	<% end %>
	</ul>
</div>

<div id="others">
	<ul>
	<% @other_tweets.each do |tweet| %>
		<li>
			<img src='<%= tweet.profile_image_url %>' />
			<span><a href="http://twitter.com/<%= tweet.from_user %>" target="_blank">@<%= tweet.from_user %></a></span>
			<span><%= tweet.text %></span>
		</li>
	<% end %>
	</ul>
</div>

</html>

